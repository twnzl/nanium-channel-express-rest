{
  "name": "nanium-channel-express-rest",
  "version": "0.1.0",
  "description": "a nanium request channel based on express using multiple REST-style endpoints",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "bundle": "npx nanium rm dist && npm run build && npx cp package.json dist && cp README.md dist && npm publish ./dist"
  },
  "keywords": [
    "nanium",
    "channel",
    "express",
    "REST"
  ],
  "files": [
    "index.js",
    "index.d.ts"
  ],
  "author": "Tobias Wenzel",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "nanium": "^0.10.2",
    "rxjs": "^6.6.7"
  },
  "devDependencies": {
    "@alex_neo/jest-expect-message": "^1.0.5",
    "@types/jest": "^27.0.2",
    "@types/jest-expect-message": "^1.0.3",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "tslint": "^6.1.3",
    "typescript": "^4.4.3"
  }
}
